datasource db {
  provider = "postgresql"
}

model user {
  id BigInt @id
}

model setting {
  key   String @id @db.VarChar
  value String @db.VarChar
}

model log_memo {
  hash      String @db.VarChar(88)
  log_ix    Int
  timestamp BigInt

  @@id([hash, log_ix])
}
